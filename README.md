# ByteCrate

ByteCrate is a Dropbox-style cloud storage application with a **Go backend**, **React + TypeScript frontend**, and a **PostgreSQL** database — all containerized with Docker for easy development.
